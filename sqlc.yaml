version: "2"
sql:
  - engine: "sqlite"
    queries: "internal/db/query.sql"
    schema: "internal/db/schema.sql"
    gen:
      go:
        package: "db"
        out: "internal/db"
        emit_prepared_queries: true
        emit_json_tags: true
